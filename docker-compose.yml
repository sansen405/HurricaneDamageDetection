version: '3.8'

services:
  inference-server:
    image: YOUR_DOCKERHUB_USERNAME/inference-server:latest
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - FLASK_ENV=production

